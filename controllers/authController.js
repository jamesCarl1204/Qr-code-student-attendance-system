const {pool} =require('./config/db')


const registerValidationRules = ''
const register_post = async (req,res) => {
    const {name, middleName, lastName, email, password, parentsEmail} = req.body;


}